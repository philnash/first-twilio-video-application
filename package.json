{
  "name": "first-twilio-video-application",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "devDependencies": {
    "twilio-run": "^2.0.0"
  },
  "engines": {
    "node": "10.17.0"
  },
  "dependencies": {
    "twilio": "^3.43.0"
  },
  "license": "MIT"
}
